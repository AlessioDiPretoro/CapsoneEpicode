@model Stones.Models.AuctionsDetails

@{
    ViewBag.Title = "Delete";
}

<h2>Elimina</h2>

<h3>Sicuro di voler eliminare? L'azione è irreversibile</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.AuctionsProducts.id)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AuctionsProducts.id)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Users.username)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Users.username)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.data)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.data)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.price)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.price)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Elimina" class="btn btn-outline-secondary" />
        </div>
    }
</div>
